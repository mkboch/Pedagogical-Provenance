# Configuration

This directory is reserved for portable experiment configuration files.

Do not commit:

- machine-specific absolute paths
- API credentials
- access tokens
- passwords
- model-cache paths
- private data locations
