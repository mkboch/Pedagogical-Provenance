# External Validation Inputs

Place locally available external validation resources under this directory.

The lecture-presentation external evaluation expects its task and ordered-corpus inputs under a local `lpm/` subdirectory.

External datasets are not redistributed through this repository.
