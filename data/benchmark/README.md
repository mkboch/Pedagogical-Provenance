# Benchmark Inputs

Place the locally available core benchmark at:

```text
data/benchmark/core_qa_benchmark.jsonl
```

Expected task fields include:

```text
task_id
task_type
question
target_doc_id
evidence_doc_ids
```

Additional sequence-policy benchmark files may be generated locally by the public benchmark builders.

Benchmark instances are not redistributed through this repository.
